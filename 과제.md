# Part0 공통기반 — 실습 과제

> 실습 노트북을 실행한 후, 동일한 노트북 안에 새 셀을 추가하여 과제를 완성하세요.
> **새 라이브러리 설치 불필요 | 과제당 소요 시간: 약 15~20분**

---

## 📒 노트북 01 — KAMP 데이터 탐색·EDA

**과제 A (필수)**
실습에서 `temperature` 컬럼으로 히스토그램을 그렸습니다.
같은 코드로 `spindle_speed` 컬럼의 히스토그램을 그리고, 분포 모양을 1줄로 설명하세요.

```python
df['spindle_speed'].hist(bins=30, figsize=(8,4))
plt.title('spindle_speed 분포')
plt.xlabel('spindle_speed')
plt.show()
# 관찰: (여기에 1줄 설명)
```

**과제 B (필수)**
`df.describe()` 출력에서 `mean`과 `max`의 차이가 가장 큰 컬럼 이름을 찾아 출력하세요.

```python
diff = df.describe().loc['max'] - df.describe().loc['mean']
print("mean-max 차이 최대 컬럼:", diff.idxmax())
```

**과제 C (심화·선택)**
`defect` 컬럼으로 그룹을 나눠 `temperature` 평균을 막대그래프로 비교하세요.

```python
df.groupby('defect')['temperature'].mean().plot(kind='bar', color=['green','red'])
plt.title('정상 vs 불량 — 평균 온도')
plt.show()
```

---

## 📒 노트북 02 — AutoML 모델 평가

**과제 A (필수)**
실습에서 `sort='Recall'`로 모델을 비교했습니다.
`sort='Accuracy'`로 바꿔 실행하고, 1위 모델이 달라지는지 확인하여 결과를 출력하세요.

**과제 B (필수)**
최적 모델의 Precision과 Recall을 출력하고, 어느 값이 더 높은지 셀 아래에 주석으로 적으세요.

**과제 C (심화·선택)**
`compare_models(n_select=3)`으로 상위 3개를 선택해 각각의 Recall을 비교하세요.

---

## 📒 노트북 03 — 모델 해석 (SHAP)

**과제 A (필수)**
실습에서 인덱스 0번 샘플의 SHAP force plot을 그렸습니다.
**인덱스를 10으로 바꿔** 동일한 plot을 다시 그리세요.

**과제 B (필수)**
SHAP summary plot에서 중요도 상위 3개 feature 이름을 셀 아래에 적어보세요.

```python
# 상위 3개 feature:
# 1위:
# 2위:
# 3위:
```

**과제 C (심화·선택)**
`plot_type='bar'`로 바꿔 막대그래프 형태의 summary plot을 그리세요.

```python
shap.summary_plot(shap_values, X_test, plot_type='bar')
```

---

## 📌 제출 방법

- 기존 노트북에 과제 셀을 추가하고 전체 실행 후 `.ipynb` 파일 제출
- **제출 기준**: 과제 A·B 실행 완료 + 결과(출력 또는 그래프) 확인 + 관찰 주석 1줄
